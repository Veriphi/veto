{
  "name": "@veto/config",
  "version": "0.0.0",
  "description": "Veto app software utils",
  "author": "Veriphi",
  "license": "PRIVATE",
  "private": true,
  "main": "out/index.js",
  "type": "out/index.d.ts",
  "scripts": {
    "postinstall": "npx tsc -p . --outDir out",
    "start": "yarn tsc -p . --outDir out --watch",
    "build": "yarn tsc -p . --outDir out",
    "tsc": "tsc"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
