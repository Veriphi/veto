{
  "name": "@veto/config",
  "version": "0.0.0",
  "description": "Veto app software utils",
  "author": "Veriphi",
  "license": "PRIVATE",
  "private": true,
  "main": "out/index.js",
  "type": "out/index.d.ts",
  "scripts": {
    "start": "tsc -p . --outDir out --watch",
    "prebuild": "rm -fr out/*",
    "build": "tsc -p . --outDir out"
  }
}
