{
  "name": "veto",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "nodemon",
    "build": "yarn workspace veto-backend build && yarn workspace veto-frontend build",
    "start": "NODE_ENV=${NODE_ENV:-dev} concurrently --kill-others-on-fail 'yarn workspace veto-backend start:watch' 'yarn workspace veto-frontend start'",
    "lint": "prettier -l '**/*.{ts,tsx,js,jsx,json}'",
    "lint-fix": "prettier --write '**/*.{ts,tsx,js,jsx,json}'"
  },
  "description": "Veto software",
  "main": "null",
  "repository": "git@github.com:Veriphi/veto.git",
  "author": "Veriphi",
  "license": "PRIVATE",
  "private": true,
  "devDependencies": {
    "husky": "^4.0.6",
    "concurrently": "^5.0.2",
    "prettier": "^1.18.2",
    "pretty-quick": "^2.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
